============
Installation
============

At the command line::

    $ pip install ynablib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ynablib
    $ pip install ynablib

Or, if you are using pipenv::

    $ pipenv install ynablib
